%Function will take the voxel parameter matrix and produce a RGB mapping of
%the directional preference of the system. 

function rgb_map = RGB_directional_map(voxel_parameter_matrix)
    %extract the coordinate po
end

